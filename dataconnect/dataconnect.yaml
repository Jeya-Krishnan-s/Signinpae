specVersion: "v1beta"
serviceId: "signin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "signin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
